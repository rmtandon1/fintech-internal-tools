/**
 * Runs are never seeded. Every row in `devin_runs` is the result of a
 * dispatch someone asked for, and a seeded run would be a run that never
 * happened.
 */
export function seedDevinRuns(): void {}
